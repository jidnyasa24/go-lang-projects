// Question: Show factored import style in Go.
// Instead of multiple 'import' lines, we use parentheses.
package main

import (
    "fmt"
    "math"
)

func main() {
    fmt.Println("Square root of 16 is:", math.Sqrt(16))
}
